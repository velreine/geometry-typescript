import { GeometryObject } from './geometry-object'

export abstract class Square extends GeometryObject {

    //private _side: number = 1;

/*    constructor(side: number) {
        super();
        this._side = side;
    }
*/


/*    public Area(): number {
        return (this._side * this._side);
    }

    public Circumference(): number {
        return (4 * this._side);
    }
*/
}
